web:
  domain: http://localhost

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/softlab?useUnicode=true & characterEncoding=utf-8 &
      useSSL=true & serverTimezone=Asia/Shanghai
    username: root
    password: fh789524560
  mvc:
    hiddenmethod:
      filter:
        enabled: true


swagger:
  enable: true

logging:
  level:
    root: info
    com.douyuehan.doubao: debug